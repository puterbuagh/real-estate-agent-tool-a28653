# Real Estate Agent Tool
Stack: nextjs-supabase-vercel-stripe
Created: 2026-05-16

## Decisions
- Next.js 14.2.33 (App Router) with strict TypeScript and `@/*` path alias — pinned to published patched release clearing the security deprecation advisory (14.2.37 was non-existent and caused ETARGET install failures)
- `next.config.js` enforces type-safety and lint correctness at build time (no `typescript.ignoreBuildErrors`, no `eslint.ignoreDuringBuilds`) so Vercel deploys fail fast on regressions
- Tailwind with AgentDesk design system: Space Grotesk display font, Inter body font, JetBrains Mono accents; navy sidebar, steel-blue primary, gradient mesh backgrounds, grain texture overlay utility
- Design tokens stored as HSL triplets in `globals.css` to pair with `hsl(var(--token))` Tailwind wrappers; elevated shadow tokens for lift states
- Print stylesheet in `globals.css` hides app chrome, forces light tokens, and exposes page-break utilities; adds `.no-print` and report-specific screen + print styles (A4 framing, page-break-inside avoid, display font for printed header/footer)
- `html, body { overflow-x: hidden }` in `globals.css` prevents global horizontal scroll; `AppShell` uses flexbox layout with fixed-width sidebar (256px) as flex child and flex-1 main content wrapper with `min-w-0` to prevent horizontal overflow; sidebar is static-positioned within the flex container (no sticky/fixed) so it renders once in document flow; mobile reverts to fixed slide-in overlay
- Sidebar component receives `mobileOpen` and `onClose` props in both mobile and desktop contexts for consistent API surface and future responsive behavior hooks; desktop ignores these props but accepts them to avoid prop-drilling conditionals in parent
- Supabase shared-schema client (browser + server) for cross-app data access
- **Supabase Auth**: Email/password authentication with middleware route protection; public routes: `/login`, `/signup`, `/auth/callback`; all other routes require active session; server checks session via `createClient()` in middleware and redirects to `/login` on auth failure
- **Supabase client export pattern**: Browser client (`lib/supabase/client.ts`) exports `createSupabaseBrowserClient` as default export (named function declaration) for consistent import convention; SignupForm and other client components import via `import createClient from '@/lib/supabase/client'` pattern
- **agent_profiles table**: PostgreSQL schema with `id` (uuid PK, references auth.users), `full_name`, `brokerage`, `email`, `phone`, `license_number`, `logo_url`, `location` (text, agent's primary market location for dynamic local stats labeling), `created_at`, `updated_at`; RLS policies: agents can read/update own row; auto-provision trigger on auth.users insert creates agent_profiles row with user email; **SELECT queries reference `full_name` column** (no `name` or `avatar_url` columns exist in schema)
- `AgentBrandingContext` hydrates from Supabase `agent_profiles` on mount (client-side fetch to `/api/profile`), saves to Supabase on `saveBranding()` (POST to `/api/profile`), and falls back to localStorage for offline/guest mode; tracks `isConfigured` (name+brokerage populated) and `hasProfile` (all required fields saved) flags; **location field is synced bidirectionally between localStorage and Supabase**; **SELECT queries use `full_name` column with tolerant fallback to legacy `name` column** in `rowToBranding` mapper; upsert payload writes to `full_name` column matching schema
- Profile page (`/profile`) server-fetches agent data via Supabase server client, renders `ProfileForm` with Supabase sync; form saves to `/api/profile` route (includes location field) with toast feedback and auto-reloads context
- Sidebar displays sign-out button with Supabase `signOut()` call (redirects to `/login`)
- FRED API as live source for mortgage rates (MORTGAGE30US + MORTGAGE15US), proxied via cached Next route with 12-week history per series
- All third-party API keys are server-only or public env vars — `FRED_API_KEY` and `ATTOM_API_KEY` are server-only (never `NEXT_PUBLIC_*`); `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` is client-accessible for Google Maps/Places integration (read directly by client code, not stored in agent profiles)
- Google Maps/Places integration uses `@googlemaps/js-api-loader` (client-side script loader) + `@types/google.maps`; Places Autocomplete enabled in Property Comparator address inputs by reading `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` directly from environment (no API route, no profile storage); graceful degradation to manual address entry when key missing or API fails; comprehensive debug logging in `lib/google-places.ts` (tagged-result API for loader errors) and `AddressInputs` component (logs autocomplete service init, input attachment, place selection with lat/lng extraction) for end-to-end troubleshooting
- **AddressInputs Google Places integration**: Uses legacy `google.maps.places.Autocomplete` API exclusively; listens for `place_changed` event, extracts `place.geometry.location` (lat/lng) and `place.formatted_address`, writes formatted_address to the native input value, stores lat/lng in hidden state, and propagates via `onChange({ address: string, lat: number, lng: number })` to parent state (lat/lng params optional for backward compat with manual entry); property-comparator page wires `onPlaceSelected` callback to capture lat/lng from Places selections, writes coordinates to address state, and passes to `/api/property-lookup` for coordinate-based ATTOM lookup
- `/api/mortgage-rate` returns `{ rate30yr: RateSeries, rate15yr: RateSeries }` with current value, week-over-week delta, and history points; `StatsGrid` is backward-compatible with this shape (legacy-shape fallback typing includes `currentDate`)
- `/api/mortgage-rate` is auth-gated via Supabase session (401 on missing session), per-user sliding-window in-memory rate limit of 30 req/min (429 with `Retry-After` header), and edge-cached with `Cache-Control: s-maxage=3600, stale-while-revalidate=86400` + `Vary: Cookie` to prevent FRED quota abuse while preserving per-user auth boundaries
- Market Stats page server-fetches the 30yr FRED rate (using server-only `FRED_API_KEY`) and passes it as a prefill prop to `MortgageCalculator` and `AffordabilityCheck` for instant-loaded live-rate defaults
- **ATTOM API integration**: ATTOM Data Solutions REST API with base URL extracted to `ATTOM_BASE_URL` constant (`https://api.gateway.attomdata.com/propertyapi/v1.0.0`); uses Property Detail (`/property/detail`) + AVM (`/avm/detail`) + Sales History (`/saleshistory/detail`) endpoints for comprehensive property data; **Property Detail provides address/beds/baths/sqft/lotSize/yearBuilt/propertySubType, AVM provides estimatedValue, Sales History provides lastSalePrice/lastSaleDate**; accepts lat/lng from Google Places Autocomplete; coordinate validation rejects invalid/missing lat/lng with `400 { ok:false, error:'Invalid coordinates', errorType:'not_found', property:null }` before API call (errorType aligned with UI state for refine-address guidance); implements multi-endpoint parallel fetch with 30s timeout + 3-attempt exponential backoff (2s→4s→8s with jitter); coordinate validation with US bounds check (24.396308°N–49.384358°N, -125.0°W–-66.93457°W); proxied through `/api/property-lookup` with consistent `{ok, data|error, errorType?}` envelope; comprehensive diagnostic logging with timestamps for coordinate validation, ATTOM request/response, and error classification
- ATTOM client configurable via `ATTOM_API_KEY` (server-only env var); Supabase schema selector uses `NEXT_PUBLIC_SUPABASE_SCHEMA` for client+server clients
- `/api/property-lookup` returns shape-specific payloads: `{ properties: ZillowProperty[] }` for POST (batch addresses with lat/lng; error responses populate `properties` array with error stubs matching GET shape), `{ property: ZillowProperty | null }` for GET (single address with lat/lng or `mlsid` query param); **errors include `errorType: 'not_found' | 'connection' | 'auth' | 'quota' | 'validation' | 'unknown'`** for granular client-side handling; 'not_found' returned for missing/invalid coordinates (aligned with refine-address UI state); 'validation' and 'invalid_address' handled as fallback variants in UI; route has 60s `maxDuration` for Vercel cold-start tolerance; coordinate validation occurs before ATTOM API call: invalid/missing lat/lng returns `400 { ok:false, error:'Invalid coordinates', errorType:'not_found', property:null }` for GET or `{ ok:false, error:'Invalid coordinates', properties:[...errorStubs] }` for POST; debug logging with timestamps uses flat string concatenation (no nested template literals in console.log) with sanitized timing (address prefix + elapsed ms), coordinate validation details (lat/lng, search mode, attempt count), and request metadata for diagnostics; **missing-key responses include populated `property` field for GET and `properties` array for POST** to maintain consistent response shape across all error paths
- `/api/property-lookup` auth is degraded-open: Supabase session check runs but logs+continues if missing in development (`NODE_ENV !== 'production'`), returns `401 { ok:false, error:'Unauthorized' }` in production; per-user sliding-window in-memory rate limit of 20 req/min returns `429 { ok:false, error:'Rate limit exceeded', errorType:'quota' }` with `Retry-After` header (note: in-memory limiter is per-instance and resets on cold start; revisit with Redis/Upstash if multi-instance accuracy required)
- **ATTOM client (`lib/attom.ts`)**: Uses `ATTOM_BASE_URL` constant (`https://api.gateway.attomdata.com/propertyapi/v1.0.0`); reads server-only `ATTOM_API_KEY` from `process.env.ATTOM_API_KEY` at request time; implements 30s timeouts, exponential backoff retry (3 attempts, 2s→4s→8s delays with jitter), coordinate validation (lat/lng within US bounds: 24.396308°N–49.384358°N, -125.0°W–-66.93457°W); **calls Property Detail + AVM + Sales History endpoints in parallel** for comprehensive data; **maps Property Detail fields (yearBuilt, lotSize, propertySubType), AVM estimatedValue, Sales History lastSalePrice/lastSaleDate** to ZillowProperty type; **`lotSize` typed as `number | null`** to match `toNumber()` return type; extensive diagnostic logging with ISO timestamps for coordinate validation, ATTOM request/response, error classification; handles ATTOM 404 (coordinates not found), 401/403 (auth), 429 (quota), network failures, and malformed responses; exports `fetchPropertyByCoordinates` as sole named export (no dead aliases); **`emptyProperty()` helper returns complete `ZillowDiagnosticDetails` structure with all fields initialized** for consistent error-stub shape
- Zod for form validation; sonner for toasts; framer-motion for staggered reveals and lift/scale interactions
- Client-side pipeline + comparisons + local market inputs state via React Context / localStorage; agent branding synced to Supabase with localStorage fallback
- Agent branding centralized in `AgentBrandingContext` with `isConfigured` (name+brokerage populated) and `hasProfile` (all required fields saved) flags; hydrates from Supabase on mount, saves via `/api/profile`, and falls back to localStorage; **location field persisted to both localStorage and Supabase agent_profiles table**; consumed by Sidebar live preview, ProfileForm validation, and ClientReport header; **SELECT queries use `full_name` column with tolerant fallback to legacy `name`** in rowToBranding mapper for backward compatibility; upsert writes to `full_name`
- **PipelineContext persistence**: `comparisons` array (stores full `ComparisonProperty[]` data per comparison, no lossy mapping) writes to localStorage on every state mutation (addComparison, updateComparisonNotes, updateComparisonClientName); `pipeline` array writes to localStorage on every mutation (addPipelineItem, updatePipelineStage, removePipelineItem); explicit `localStorage.setItem` calls in all mutator functions ensure immediate persistence
- PipelineContext tracks extended entries (price, clientName, notes, stageEnteredAt) and auto-stamps `stageEnteredAt` on stage transitions for days-in-stage tracking
- PipelineContext exposes `pipeline`, `updatePipelineStage`, `addPipelineItem`, `removePipelineItem`, `addComparison(properties, label?)` with auto-computed winner, plus `getComparisonById`, `updateComparisonNotes`, `updateComparisonClientName` for report editing
- `addComparison` is invoked with positional `(properties: ComparisonProperty[], label?: string)` args (not an object literal) — strict TS enforced in Vercel builds
- Saved comparisons persist `reportNotes` (per-property) and `clientName` for reuse in Client Report
- Local market inputs (median price, DOM, inventory, YoY, mortgage rate) persisted to localStorage with edit/save toggle; shape extracted as reusable `LocalMarketInputsData` type; **LocalMarketInputs reads `branding.location` from AgentBrandingContext to display dynamic location label** (e.g., "Median Days on Market in {location}") instead of hardcoded region references
- Mortgage math helpers (`calcMonthlyPayment`, `maxHomePriceForIncome`) live in `lib/utils` and assume 28% DTI for affordability
- Kanban drag-and-drop uses native HTML5 DnD (no external lib) for lightweight stage transitions with scale/shadow lift states
- Honeypot pattern for spam protection on quick-add forms
- Prefer `type` aliases over empty `interface` extensions to satisfy strict lint
- `/client-report` opts out of static prerender (`export const dynamic = 'force-dynamic'` + `revalidate = 0`); the `useSearchParams`-consuming inner client component is wrapped in a `Suspense` boundary to satisfy Next 14 CSR-bailout prerender rules on Vercel
- Profile page (`/profile`) server-fetches agent data via Supabase and renders `ProfileForm` with Supabase sync via `/api/profile`; named function declaration pattern for default export satisfies static analysis
- All layout and dashboard components use explicit `export default` statements (not inline default exports) for consistent module resolution; named exports removed to prevent import ambiguity
- Dashboard and all child components constrained with `overflow-x-hidden`, `max-w-full`, and `min-w-0` guards to prevent horizontal overflow; table components scroll horizontally within their containers
- `Input` component is a named export from `components/ui/Input.tsx` (not default export) to maintain consistent import patterns across the codebase
- **FRED rate labeling**: Dashboard `StatsGrid` displays national mortgage rates from FRED API with accurate '30-Year Mortgage Rate' label (not region-specific) since FRED MORTGAGE30US series provides US national averages
- **Logging conventions**: All diagnostic logging in `/api/property-lookup/route.ts` uses flat string concatenation (no nested template literals) to avoid syntax errors; address prefixes are sanitized (first 10 chars) for privacy; timestamps use ISO format; coordinate validation logs include lat/lng, search mode, attempt count with properly balanced parentheses and terminated strings
- **Error type alignment**: API route returns `errorType: 'not_found'` for missing/invalid coordinates (matching UI refine-address state); `PropertyErrorCard` handles 'validation' and 'invalid_address' as fallback variants that show refine-address guidance
- **Response shape consistency**: All `/api/property-lookup` error responses maintain consistent shape: GET returns `{ ok:false, error:string, errorType:string, property:null }`, POST returns `{ ok:false, error:string, errorType:string, properties:[...errorStubs] }` where each stub includes the original address for client-side reconciliation; missing-key responses populate the `property`/`properties` fields to prevent downstream null-ref errors
- **Address display deduplication**: PropertyCard uses `cleanAddress()` helper to remove city/state/zip suffixes from formatted addresses when those components are already displayed separately, preventing visual duplication (e.g., "123 Main St, Austin, TX" → "123 Main St" when city/state shown in separate fields)

## Components
- Auth: `AuthCard` (gradient-bordered card with display-font header + eyebrow text), `LoginForm` (email/password with Zod validation, Supabase signInWithPassword, toast feedback, redirect to `/`), `SignupForm` (email/password/full_name with Zod validation, imports `createSupabaseBrowserClient` as default export from `lib/supabase/client`, calls Supabase signUp + agent_profiles upsert via trigger, toast feedback, redirect to `/`)
- Layout: `middleware.ts` (Supabase auth check on all routes except `/login`, `/signup`, `/auth/callback`; redirects to `/login` on missing session), `app/(auth)/layout.tsx` (centered auth card with gradient mesh background), `AppShell` (explicit `export default AppShell` statement; flexbox layout with fixed-width sidebar (256px flex-shrink-0) and flex-1 main content wrapper with `min-w-0` to prevent horizontal overflow; sidebar receives `mobileOpen` and `onClose` props in both mobile and desktop contexts for unified API surface; sidebar is static-positioned within flex container for single render in document flow; mobile reverts to fixed overlay; wraps `TopBar` + `<main>`; no horizontal scroll), `Sidebar` (explicit `export default Sidebar` statement; accepts `mobileOpen` and `onClose` props for both responsive contexts; static-positioned within flexbox parent on desktop (≥1024px), fixed overlay on mobile; w-64, dark-navy nav with versioned wordmark, mono-cased section labels, live agent logo/initials preview with gradient glow + Settings section showing unsaved-profile nudge if `isConfigured` but not `hasProfile` + sign-out button with Supabase `signOut()` call; explicit height constraints on header/footer to prevent flex overflow; inline `zIndex: 50` for explicit stacking above main on mobile), `TopBar` (default export; sticky backdrop-blur, mono-uppercase date, live pulse indicator + page title; `overflow-x-hidden` + `max-w-full` + responsive padding)
- Pages: Login (`/login`, renders `LoginForm` in `AuthCard`), Signup (`/signup`, renders `SignupForm` in `AuthCard`), Dashboard (`/`, gradient mesh background + oversized editorial headline + grain overlay; constrained with `overflow-x-hidden` + `max-w-7xl mx-auto` for contained width; imports `StatsGrid`, `RecentComparisons`, `QuickAddToPipeline` as default exports), Property Comparator (2–5 addresses or MLS IDs with toggle; Google Places Autocomplete integration using legacy `google.maps.places.Autocomplete` API; page-level `onPlaceSelected` callback captures lat/lng from `place.geometry.location`, writes coordinates to address state `{ address: string, lat: number, lng: number }`, and passes to `/api/property-lookup` for coordinate-based ATTOM lookup via multi-endpoint fetch (Property Detail + AVM + Sales History); coordinate validation in route handler rejects invalid lat/lng with 400 { errorType:'not_found', property:null } before calling ATTOM API (aligned with UI refine-address state); comprehensive debug logging with timestamps in AddressInputs (logs loader result, autocomplete init, input attachment, place selection with lat/lng extraction) and route handler (coordinate validation with flat string concatenation, search mode, attempt count); graceful degradation to manual entry if key missing/API fails with soft warning for freeform addresses bypassing dropdown (no coordinates available for manual input); elapsed-time loading indicator + slow-cold-start hint after 20s; parallel lookup with 30s timeout + 3-attempt exponential backoff (2s→4s→8s); coordinate validation with inline hints; per-card exponential-backoff Retry button; bulk Retry-failed action for multi-card errors; staggered framer-motion reveals; calls `addComparison(properties, label?)` with positional args; save/clear/print), Pipeline (kanban with summary bar + inline add form + DnD across stages with drag scale states + gradient column underlines + display-font cards), Market Stats (server-fetched live 30yr rate prefill via server-only `FRED_API_KEY` + gradient mesh background + underlined section headings + local inputs + mortgage calc + affordability), Profile (`/profile`, server-fetches agent data via Supabase, renders `ProfileForm` with Supabase sync via `/api/profile` + toast feedback + 'Saved' indicator; **includes location field with MapPin icon for dynamic local stats labeling; location saved to both localStorage and Supabase**), Client Report (`/client-report`, force-dynamic + Suspense-wrapped inner component reading `?comparisonId=`, saved-comparison dropdown + agent branding + printable preview with display-font hierarchy), Email Report (stub)
- Dashboard widgets: `StatsGrid` (default export; staggered framer-motion reveals, live FRED 30yr rate via new API shape with legacy fallback typing incl. `currentDate`, **national rate labeling**: '30-Year Mortgage Rate' (not region-specific) reflecting FRED's US national average data; constrained with `overflow-x-hidden` + responsive grid), `StatCard` (gradient accent rule, hover lift with primary glow, shimmer skeleton), `RecentComparisons` (default export; gradient header, mono-uppercase eyebrows, animated row reveals, structured winner cell; table scrolls horizontally inside container with `overflow-x-auto` on wrapper, `max-w-full` guard; reads full `Comparison` type with `ComparisonProperty[]` array directly from PipelineContext without id-based routing), `QuickAddToPipeline` (default export; display-font labels, gradient accents, refined focus states; **responsive grid with `minmax(120px, 200px)` stage column** for balanced layout on narrow viewports; `min-w-0` on form grid for contained width)
- Comparator: `AddressInputs` (dynamic 2–5 with MLS-ID toggle + Google Places Autocomplete using legacy `google.maps.places.Autocomplete` API; listens for `place_changed` event, extracts `place.geometry.location` (lat/lng) and `place.formatted_address`, writes formatted_address to native input value, stores lat/lng in hidden state, and propagates via `onChange({ address: string, lat: number, lng: number })` to parent for coordinate-based ATTOM lookup; lat/lng params optional for backward compat with manual entry; imports `Input` as named export from `components/ui/Input.tsx`; debug logging with timestamps for loader result, autocomplete init, input attachment, and place selection with lat/lng extraction; manual text input only triggers onChange when autocomplete inactive or bypassed (no coordinates available for manual input); memoized `loadGoogleMaps`/`loadGooglePlaces` helpers with loading/error states; soft warning when user submits freeform address bypassing dropdown; client-side validation + inline format hints: coordinates required for address mode via Places Autocomplete; numeric-only for MLS), `PropertySkeletonCard`, `PropertyCard` (default export; Best/Highest Value badges, image gradient overlay, display-font typography; **displays estimatedValue (from ATTOM AVM), lastSalePrice/lastSaleDate (from Sales History), yearBuilt, lotSize, propertySubType**; **uses `cleanAddress()` helper to dedupe city/state/zip in displayed addresses** — removes city/state/zip suffix from formatted address when those components are already shown separately, preventing visual duplication; **imports `formatDate` from `lib/utils` for lastSaleDate display**), `PropertyErrorCard` (default export; **granular errorType-based UI aligned with API**: yellow informational for 'not_found' (missing/invalid coordinates) with refine-address guidance + "verify address" + ATTOM link + diagnostic details panel showing coordinates used for troubleshooting; handles 'validation'/'invalid_address' as fallback variants with same refine-address treatment; red actionable for connection/auth/quota with specific remediation steps; exponential-backoff Retry button using last-known search mode and attempt count state), `ComparisonTable` (display-font headers, gradient zebra striping, display-font numerics, scannable metrics, Best Value & Highest Value highlights, red >60 DOM), `ClientReport` (printable branded modal with per-property pages + display-font hierarchy on header/address/price)
- Pipeline: `AddPropertyForm` (inline address/price/client/stage/notes), `PipelineSummaryBar` (accent-bordered cards, total value, per-stage counts, longest-sitting deal), `PipelineCard` (draggable, display-font typography, days-in-stage badge, drag-state shadow lift, expandable notes, delete), `KanbanBoard` (4 stage columns with HTML5 drag-and-drop + gradient column underlines)
- Market Stats: `Sparkline` (token-styled inline SVG), `RateDisplay` (current rate, WoW delta glow + gradient top accent, sparkline via correct `data` prop, display-font emphasis), `LiveRatesSection` (fetches /api/mortgage-rate with skeleton+retry, renders 30yr/15yr cards + FRED source caption), `LocalMarketInputs` (zod-validated, localStorage-persisted, edit/save toggle with 5 stat cards; **reads `branding.location` from AgentBrandingContext** to display dynamic location label (e.g., "Median Days on Market in {location}") instead of hardcoded region references), `MortgageCalculator` (real-time P&I, live-rate prefill prop, 15/30yr term, display-font result panels with gradient accents), `AffordabilityCheck` (28% DTI max-price calc with live-rate prefill prop + display-font emphasis + atmospheric gradient accents + assumptions panel)
- Client Report: `ComparisonSelector` (dropdown of saved comparisons), `ReportPreview` (branded printable layout with display-font header/footer pulling from AgentBrandingContext), `PropertySummaryBlock` (photo, address, Price|Beds|Baths|Sqft|$/sqft, Best/Highest Value badges, editable per-property notes), `ReportActions` (Print/Save-as-PDF + Copy shareable link with sonner toast, hidden during print)
- Profile: `ProfileForm` (agent name, brokerage, email, phone, license, **location (with MapPin icon for dynamic local stats labeling; saved to localStorage and Supabase)**, logo upload; zod validation; saves to Supabase via `/api/profile`; success toast + 'Saved' indicator; named function declaration default export)
- UI primitives: `Button`, `Input` (named export from `components/ui/Input.tsx`), `Select`, `Card` (+ subcomponents; optional grain texture + elevated shadow tokens for lift states), `Skeleton`, `EmptyState`
- State: `AgentBrandingContext` (centralized branding state with `isConfigured` and `hasProfile` flags; hydrates from Supabase on mount via `/api/profile`, saves via POST to `/api/profile`, falls back to localStorage; **includes `location` field synced bidirectionally between localStorage and Supabase**; **SELECT queries use `full_name` column with tolerant fallback to legacy `name` in `rowToBranding` mapper** for backward compatibility; upsert payload writes to `full_name` matching schema; consumed by Sidebar, Profile, ClientReport, LocalMarketInputs), `PipelineContext` (extended pipeline entries + saved comparisons (stores full `ComparisonProperty[]` array per comparison, no lossy mapping) with reportNotes/clientName/winner, localStorage-backed; **explicit `localStorage.setItem` writes in all mutator functions** (addComparison, updateComparisonNotes, updateComparisonClientName, addPipelineItem, updatePipelineStage, removePipelineItem) for immediate persistence; exposes getComparisonById/updateComparisonNotes/updateComparisonClientName)
- API: `/api/profile` (GET fetches agent_profiles row via Supabase session user; POST updates agent_profiles row **including location field** with Zod validation; **uses `agent_profiles.id` primary key** for consistent updates; returns 401 on missing session), `/api/mortgage-rate` (cached FRED proxy using server-only `FRED_API_KEY`, returns 30yr+15yr series with 12-week history + WoW deltas; Supabase session auth-gated — 401 on missing session; per-user sliding-window rate limit 30 req/min — 429 with `Retry-After`; edge-cached `s-maxage=3600, stale-while-revalidate=86400` + `Vary: Cookie`), `/api/property-lookup` (ATTOM via server-only `ATTOM_API_KEY` read from `process.env.ATTOM_API_KEY`; uses `ATTOM_BASE_URL` constant; degraded-open auth — logs+continues if no session in dev, 401 in prod; per-user sliding-window rate limit 20 req/min — 429 with `Retry-After` + `errorType:'quota'`; 60s maxDuration for cold-start tolerance; validates coordinates before ATTOM API call: returns `400 { ok:false, error:'Invalid coordinates', errorType:'not_found', property:null }` for GET or `{ ok:false, error:'Invalid coordinates', properties:[...errorStubs] }` for POST with invalid/missing lat/lng (aligned with UI refine-address state); debug logging with ISO timestamps uses flat string concatenation (no nested template literals) with sanitized timing (address prefix + elapsed ms), coordinate validation (lat/lng, search mode, attempt count with balanced parentheses and terminated strings), and request metadata for diagnostics; POST→`{ properties: ZillowProperty[] }` batch address+lat/lng lookups via multi-endpoint parallel fetch (Property Detail + AVM + Sales History); error responses populate `properties` array with error stubs matching GET shape; GET→`{ property: ZillowProperty | null }` for `?address=&lat=&lng=` (coordinate-based) or `?mlsid=` single lookup; **returns errorType for granular client handling** ('not_found' for missing/invalid coordinates, 'validation'/'invalid_address' as fallback variants); **all error responses include populated `property`/`properties` fields** to maintain consistent shape; error stubs align with ZillowProperty schema), `/auth/callback` (Supabase auth code exchange route)
- Lib: `lib/fred.ts` (server-only — reads `FRED_API_KEY` at request time; multi-series helper), `lib/attom.ts` (server-only — reads `ATTOM_API_KEY` from `process.env.ATTOM_API_KEY` at request time; uses `ATTOM_BASE_URL` constant (`https://api.gateway.attomdata.com/propertyapi/v1.0.0`); exports `fetchPropertyByCoordinates` as sole named export (no dead aliases); **calls Property Detail + AVM + Sales History endpoints in parallel for comprehensive property data**; **maps yearBuilt, lotSize (typed as `number | null`), propertySubType from Property Detail; estimatedValue from AVM; lastSalePrice, lastSaleDate from Sales History**; **`emptyProperty()` helper returns complete `ZillowDiagnosticDetails` structure with all fields initialized** for consistent error-stub shape across all error paths; implements 30s timeout, 3-attempt exponential backoff with jitter (2s→4s→8s), coordinate validation (US bounds: 24.396308°N–49.384358°N, -125.0°W–-66.93457°W), granular error classification with detailed diagnostic logging with ISO timestamps for coordinate validation, ATTOM request/response, error classification), `lib/google-places.ts` (client-side memoized helpers: `loadGoogleMaps` uses tagged-result API (`{ ok: true, loader }` or `{ ok: false, reason: 'noKey' | 'error', error? }`) with debug logging for `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` presence + loader init; loads `@googlemaps/js-api-loader` with `places` library; `loadGooglePlaces` returns Places Autocomplete service; includes loading/error states for graceful degradation), `lib/utils.ts` (cn, currency/date/number/percent formatters, `daysBetween`, `calcMonthlyPayment`, `maxHomePriceForIncome`, **`formatDate`** (exported for PropertyCard lastSaleDate display), **`cleanAddress`** (helper that removes city/state/zip suffix from formatted addresses when those components are already displayed separately, preventing visual duplication)), `lib/supabase/client.ts` (exports `createSupabaseBrowserClient` as default export — named function declaration pattern for browser client; reads `NEXT_PUBLIC_SUPABASE_SCHEMA`), `lib/supabase/server.ts` (server client reads `NEXT_PUBLIC_SUPABASE_SCHEMA`; used by middleware, profile route, property-lookup + mortgage-rate routes for session-based auth check), `lib/supabase/migrations/001_agent_profiles.sql` (agent_profiles table schema with `full_name` column, RLS policies + auto-provision trigger on auth.users insert), `lib/supabase/migrations/002_add_location_to_agent_profiles.sql` (adds location text column to agent_profiles for dynamic local stats labeling)
- Types: shared `types/index.ts` (incl. `ComparisonProperty` (with `lotSize: number | null` typed to match `toNumber()` return), `Comparison` type (exported; contains `ComparisonProperty[]` with full property data), `ComparisonResult` with reportNotes+clientName, extended `PipelineEntry`, `RateSeries`, `LocalMarketInputs`, `LocalMarketInputsData`, `MortgageCalc*`, `AgentBranding` (**includes location field; no googleApiKey field**), `GooglePlace` with place_id+description+formatted_address+geometry.location (lat/lng), `AddressInputValue` union (string | { address: string; lat: number; lng: number }), plus type guards `isCoordinateInput`/`isMLSId`; `ZillowProperty` type represents ATTOM-backed property records **including yearBuilt, lotSize (typed as `number | null`), propertySubType, estimatedValue, lastSalePrice, lastSaleDate**)
- Config: Tailwind tokens (Space Grotesk display, Inter body, JetBrains Mono accents, elevated shadow tokens, grain overlay utility), PostCSS, `next.config.js` (strict builds — no TS/ESLint bypass flags), `package.json` (next pinned to 14.2.33 — valid published security patch; `@googlemaps/js-api-loader` + `@types/google.maps` for Places integration), `.env.example` (Supabase shared-schema via `NEXT_PUBLIC_SUPABASE_SCHEMA` + `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` + server-only `SUPABASE_SERVICE_ROLE_KEY`, `FRED_API_KEY`, `ATTOM_API_KEY` (server-only for ATTOM Data Solutions API, replaces deprecated `RAPIDAPI_KEY`); `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` for client-side Google Maps/Places (no profile storage, read directly by client); note that Supabase auth is required; expanded Google Places troubleshooting with legacy `Autocomplete` API usage, debug logging with timestamps in lib/google-places.ts + AddressInputs with lat/lng extraction from place.geometry.location, and step-by-step inspection guide for key presence → loader init → autocomplete init → input attachment → place selection; comprehensive ATTOM troubleshooting with coordinate-based lookup via multi-endpoint parallel fetch (Property Detail + AVM + Sales History) + coordinate validation in route handler before API call returning errorType:'not_found' for invalid coordinates (aligned with refine-address UI state) + diagnostic logging with timestamps in route handler (coordinate validation with flat string concatenation, search mode, attempt count) and lib/attom.ts (ATTOM request/response details) for troubleshooting not-found results, Google Places Autocomplete using legacy `google.maps.places.Autocomplete` API with place_changed event for lat/lng extraction via page-level `onPlaceSelected` callback writing coordinates to address state, graceful degradation, and actionable steps for not-found vs connection/auth/quota errors mapped to yellow/red card states with aligned errorType ('not_found' → refine-address, 'validation'/'invalid_address' → fallback refine-address)), README (documents Supabase auth flow with login/signup pages, middleware route protection, agent_profiles table schema with **`full_name` column** (not `name`), RLS policies + auto-provision trigger + **location field for dynamic local stats labeling (synced to localStorage + Supabase)**, server-only vs public env var conventions + `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` direct-read pattern with no profile storage or API route + expanded Property Comparator section: Vercel cold-start expectations with 60s route timeout, exponential-backoff retry strategy, per-card vs bulk retry UX, coordinate-based lookup via Google Places with coordinate validation before API call returning errorType:'not_found' for invalid coordinates (aligned with refine-address UI state) + diagnostic logging with timestamps in route handler (coordinate validation with flat string concatenation for syntax safety, search mode, attempt count) and lib/attom.ts (ATTOM request/response details) for troubleshooting not-found results, Google Places Autocomplete using legacy `google.maps.places.Autocomplete` API with place_changed event for lat/lng extraction via page-level `onPlaceSelected` callback writing coordinates to address state, graceful degradation, and actionable steps for not-found vs connection/auth/quota errors mapped to yellow/red card states with aligned errorType ('not_found' → refine-address, 'validation'/'invalid_address' → fallback refine-address)), `globals.css` (`html, body { overflow-x: hidden }` for global horizontal scroll prevention; print media query hides nav/topbar, forces light tokens, exposes page-break utilities)