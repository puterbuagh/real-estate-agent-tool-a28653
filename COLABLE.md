# Real Estate Agent Tool
Stack: nextjs-supabase-vercel-stripe
Created: 2026-05-16

## Decisions
- Next.js 14.2.33 (App Router) with strict TypeScript and `@/*` path alias — pinned to published patched release clearing the security deprecation advisory (14.2.37 was non-existent and caused ETARGET install failures)
- `next.config.js` enforces type-safety and lint correctness at build time (no `typescript.ignoreBuildErrors`, no `eslint.ignoreDuringBuilds`) so Vercel deploys fail fast on regressions
- Tailwind with AgentDesk design system: Space Grotesk display font, Inter body font, JetBrains Mono accents; navy sidebar, steel-blue primary, gradient mesh backgrounds, grain texture overlay utility
- Design tokens stored as HSL triplets in `globals.css` to pair with `hsl(var(--token))` Tailwind wrappers; elevated shadow tokens for lift states
- Print stylesheet in `globals.css` hides app chrome, forces light tokens, and exposes page-break utilities; adds `.no-print` and report-specific screen + print styles (A4 framing, page-break-inside avoid, display font for printed header/footer)
- `html, body { overflow-x: hidden }` in `globals.css` prevents global horizontal scroll; `AppShell` uses flexbox layout with fixed-width sidebar (256px) as flex child and flex-1 main content wrapper with `min-w-0` to prevent horizontal overflow; sidebar is static-positioned within the flex container (no sticky/fixed) so it renders once in document flow; mobile reverts to fixed slide-in overlay
- Sidebar component receives `mobileOpen` and `onClose` props in both mobile and desktop contexts for consistent API surface and future responsive behavior hooks; desktop ignores these props but accepts them to avoid prop-drilling conditionals in parent
- Supabase shared-schema client (browser + server) for cross-app data access
- FRED API as live source for mortgage rates (MORTGAGE30US + MORTGAGE15US), proxied via cached Next route with 12-week history per series
- All third-party API keys are server-only or public env vars — `FRED_API_KEY` and `RAPIDAPI_KEY` are server-only (never `NEXT_PUBLIC_*`); `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` is client-accessible for Google Maps/Places integration (read directly by client code, not stored in agent profiles)
- Google Maps/Places integration uses `@googlemaps/js-api-loader` (client-side script loader) + `@types/google.maps`; Places Autocomplete enabled in Property Comparator address inputs by reading `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` directly from environment (no API route, no profile storage); graceful degradation to manual address entry when key missing or API fails; **comprehensive debug logging** added to `lib/google-places.ts` (tagged-result API for loader errors) and `AddressInputs` component (logs autocomplete service init, input attachment, place selection) for end-to-end troubleshooting
- `/api/mortgage-rate` returns `{ rate30yr: RateSeries, rate15yr: RateSeries }` with current value, week-over-week delta, and history points; `StatsGrid` is backward-compatible with this shape (legacy-shape fallback typing includes `currentDate`)
- `/api/mortgage-rate` is auth-gated via Supabase session (401 on missing session), per-user sliding-window in-memory rate limit of 30 req/min (429 with `Retry-After` header), and edge-cached with `Cache-Control: s-maxage=3600, stale-while-revalidate=86400` + `Vary: Cookie` to prevent FRED quota abuse while preserving per-user auth boundaries
- Market Stats page server-fetches the 30yr FRED rate (using server-only `FRED_API_KEY`) and passes it as a prefill prop to `MortgageCalculator` and `AffordabilityCheck` for instant-loaded live-rate defaults
- Zillow data via RapidAPI host `zillow-com-live-data-scraper-api.p.rapidapi.com` (switched from prior host); supports address lookup and MLS ID lookup (`/bymlsid`); proxied through `/api/property-lookup` with consistent `{ok, data|error, errorType?}` envelope
- RapidAPI host configurable via `NEXT_PUBLIC_RAPIDAPI_ZILLOW_HOST` (host string is non-sensitive); Supabase schema selector uses `NEXT_PUBLIC_SUPABASE_SCHEMA` for client+server clients
- `/api/property-lookup` returns shape-specific payloads: `{ properties }` for POST (batch addresses), `{ property }` for GET (single address or `mlsid` query param); errors include `errorType: 'not_found' | 'connection' | 'auth' | 'quota' | 'validation' | 'unknown'` for granular client-side handling; route has 60s `maxDuration` for Vercel cold-start tolerance and logs sanitized timing (address prefix + elapsed ms) for diagnostics
- `/api/property-lookup` auth is degraded-open: Supabase session check runs but logs+continues if missing in development (`NODE_ENV !== 'production'`), returns `401 { ok:false, error:'Unauthorized' }` in production; per-user sliding-window in-memory rate limit of 20 req/min returns `429 { ok:false, error:'Rate limit exceeded', errorType:'quota' }` with `Retry-After` header (note: in-memory limiter is per-instance and resets on cold start; revisit with Redis/Upstash if multi-instance accuracy required)
- Zillow client (`lib/zillow.ts`) implements 30s timeouts, exponential backoff retry (3 attempts, 2s→4s→8s delays with jitter), address validation (rejects < 10 chars, missing state/ZIP patterns), and granular error classification; handles RapidAPI 404 (address not found), 401/403 (auth), 429 (quota), network failures, and malformed responses with detailed logging (timestamps + address prefix + attempt number + error type)
- Zod for form validation; sonner for toasts; framer-motion for staggered reveals and lift/scale interactions
- Client-side pipeline + comparisons + local market inputs + agent branding state via React Context / localStorage (no auth yet for app state, but property-lookup + mortgage-rate APIs now auth-gated in prod, degraded-open in dev)
- Agent branding centralized in `AgentBrandingContext` with `isConfigured` (name+brokerage populated) and `hasProfile` (all required fields saved) flags; consumed by Sidebar live preview, ProfileForm validation, and ClientReport header
- PipelineContext tracks extended entries (price, clientName, notes, stageEnteredAt) and auto-stamps `stageEnteredAt` on stage transitions for days-in-stage tracking
- PipelineContext exposes `pipeline`, `updatePipelineStage`, `addPipelineItem`, `removePipelineItem`, `addComparison(properties, label?)` with auto-computed winner, plus `getComparisonById`, `updateComparisonNotes`, `updateComparisonClientName` for report editing
- `addComparison` is invoked with positional `(properties: ComparisonProperty[], label?: string)` args (not an object literal) — strict TS enforced in Vercel builds
- Saved comparisons persist `reportNotes` (per-property) and `clientName` for reuse in Client Report
- Local market inputs (median price, DOM, inventory, YoY, mortgage rate) persisted to localStorage with edit/save toggle; shape extracted as reusable `LocalMarketInputsData` type
- Mortgage math helpers (`calcMonthlyPayment`, `maxHomePriceForIncome`) live in `lib/utils` and assume 28% DTI for affordability
- Kanban drag-and-drop uses native HTML5 DnD (no external lib) for lightweight stage transitions with scale/shadow lift states
- Honeypot pattern for spam protection on quick-add forms
- Prefer `type` aliases over empty `interface` extensions to satisfy strict lint
- `/client-report` opts out of static prerender (`export const dynamic = 'force-dynamic'` + `revalidate = 0`); the `useSearchParams`-consuming inner client component is wrapped in a `Suspense` boundary to satisfy Next 14 CSR-bailout prerender rules on Vercel
- Profile page (`/profile`) uses `ProfileForm` directly with local-storage-only status card; named function declaration pattern for default export satisfies static analysis
- All layout and dashboard components use explicit `export default` statements (not inline default exports) for consistent module resolution; named exports removed to prevent import ambiguity
- Dashboard and all child components constrained with `overflow-x-hidden`, `max-w-full`, and `min-w-0` guards to prevent horizontal overflow; table components scroll horizontally within their containers

## Components
- Layout: `AppShell` (explicit `export default AppShell` statement; flexbox layout with fixed-width sidebar (256px flex-shrink-0) and flex-1 main content wrapper with `min-w-0` to prevent horizontal overflow; sidebar receives `mobileOpen` and `onClose` props in both mobile and desktop contexts for unified API surface; sidebar is static-positioned within flex container for single render in document flow; mobile reverts to fixed overlay; wraps `TopBar` + `<main>`; no horizontal scroll), `Sidebar` (explicit `export default Sidebar` statement; accepts `mobileOpen` and `onClose` props for both responsive contexts; static-positioned within flexbox parent on desktop (≥1024px), fixed overlay on mobile; w-64, dark-navy nav with versioned wordmark, mono-cased section labels, live agent logo/initials preview with gradient glow + Settings section showing unsaved-profile nudge if `isConfigured` but not `hasProfile`; explicit height constraints on header/footer to prevent flex overflow; inline `zIndex: 50` for explicit stacking above main on mobile), `TopBar` (default export; sticky backdrop-blur, mono-uppercase date, live pulse indicator + page title; `overflow-x-hidden` + `max-w-full` + responsive padding)
- Pages: Dashboard (`/`, gradient mesh background + oversized editorial headline + grain overlay; constrained with `overflow-x-hidden` + `max-w-7xl mx-auto` for contained width; imports `StatsGrid`, `RecentComparisons`, `QuickAddToPipeline` as default exports), Property Comparator (2–5 addresses or MLS IDs with toggle; Google Places Autocomplete integration via `loadGoogleMaps` client helper reading `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` directly; **comprehensive debug logging** in AddressInputs (logs loader result, autocomplete service init, input attachment, place selection) for troubleshooting; graceful degradation to manual entry if key missing/API fails with soft warning for freeform addresses bypassing dropdown; elapsed-time loading indicator + slow-cold-start hint after 20s; parallel lookup with 30s timeout + 3-attempt exponential backoff (2s→4s→8s); address/MLS validation with inline hints; per-card exponential-backoff Retry button; bulk Retry-failed action for multi-card errors; staggered framer-motion reveals; calls `addComparison(properties, label?)` with positional args; save/clear/print), Pipeline (kanban with summary bar + inline add form + DnD across stages with drag scale states + gradient column underlines + display-font cards), Market Stats (server-fetched live 30yr rate prefill via server-only `FRED_API_KEY` + gradient mesh background + underlined section headings + local inputs + mortgage calc + affordability), Profile (`/profile`, renders `ProfileForm` with local-storage-only status card for name/brokerage/email/phone/license/logo), Client Report (`/client-report`, force-dynamic + Suspense-wrapped inner component reading `?comparisonId=`, saved-comparison dropdown + agent branding + printable preview with display-font hierarchy), Email Report (stub)
- Dashboard widgets: `StatsGrid` (default export; staggered framer-motion reveals, live FRED 30yr rate via new API shape with legacy fallback typing incl. `currentDate`, Florida labeling; constrained with `overflow-x-hidden` + responsive grid), `StatCard` (gradient accent rule, hover lift with primary glow, shimmer skeleton), `RecentComparisons` (default export; gradient header, mono-uppercase eyebrows, animated row reveals, structured winner cell; table scrolls horizontally inside container with `overflow-x-auto` on wrapper, `max-w-full` guard), `QuickAddToPipeline` (default export; display-font labels, gradient accents, refined focus states; `min-w-0` on form grid + responsive column layout to prevent overflow)
- Comparator: `AddressInputs` (dynamic 2–5 with MLS-ID toggle + Google Places Autocomplete via `@googlemaps/js-api-loader` reading `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` directly; **debug logging** for loader result tag (ok/noKey/error), autocomplete service init, input attachment, and place selection with place_id; memoized `loadGoogleMaps`/`loadGooglePlaces` helpers with loading/error states; soft warning when user submits freeform address bypassing dropdown; client-side validation + inline format hints: 10-char min, state/ZIP required for addresses; numeric-only for MLS), `PropertySkeletonCard`, `PropertyCard` (default export; Best/Highest Value badges, image gradient overlay, display-font typography), `PropertyErrorCard` (default export; granular errorType-based UI with tailored troubleshooting: yellow informational for not_found with "verify address/MLS ID" + Zillow link, red actionable for connection/auth/quota with specific remediation steps; exponential-backoff Retry button using last-known search mode and attempt count state), `ComparisonTable` (display-font headers, gradient zebra striping, display-font numerics, scannable metrics, Best Value & Highest Value highlights, red >60 DOM), `ClientReport` (printable branded modal with per-property pages + display-font hierarchy on header/address/price)
- Pipeline: `AddPropertyForm` (inline address/price/client/stage/notes), `PipelineSummaryBar` (accent-bordered cards, total value, per-stage counts, longest-sitting deal), `PipelineCard` (draggable, display-font typography, days-in-stage badge, drag-state shadow lift, expandable notes, delete), `KanbanBoard` (4 stage columns with HTML5 drag-and-drop + gradient column underlines)
- Market Stats: `Sparkline` (token-styled inline SVG), `RateDisplay` (current rate, WoW delta glow + gradient top accent, sparkline via correct `data` prop, display-font emphasis), `LiveRatesSection` (fetches /api/mortgage-rate with skeleton+retry, renders 30yr/15yr cards + FRED source caption), `LocalMarketInputs` (zod-validated, localStorage-persisted, edit/save toggle with 5 stat cards), `MortgageCalculator` (real-time P&I, live-rate prefill prop, 15/30yr term, display-font result panels with gradient accents), `AffordabilityCheck` (28% DTI max-price calc with live-rate prefill prop + display-font emphasis + atmospheric gradient accents + assumptions panel)
- Client Report: `ComparisonSelector` (dropdown of saved comparisons), `ReportPreview` (branded printable layout with display-font header/footer pulling from AgentBrandingContext), `PropertySummaryBlock` (photo, address, Price|Beds|Baths|Sqft|$/sqft, Best/Highest Value badges, editable per-property notes), `ReportActions` (Print/Save-as-PDF + Copy shareable link with sonner toast, hidden during print)
- Profile: `ProfileForm` (agent name, brokerage, email, phone, license, logo upload; zod validation; localStorage-persisted; success toast + 'Saved' indicator; named function declaration default export)
- UI primitives: `Button`, `Input`, `Select`, `Card` (+ subcomponents; optional grain texture + elevated shadow tokens for lift states), `Skeleton`, `EmptyState`
- State: `AgentBrandingContext` (centralized branding state with `isConfigured` and `hasProfile` flags, localStorage-backed; consumed by Sidebar, Profile, ClientReport), `PipelineContext` (extended pipeline entries + saved comparisons with reportNotes/clientName/winner, localStorage-backed; exposes getComparisonById/updateComparisonNotes/updateComparisonClientName)
- API: `/api/mortgage-rate` (cached FRED proxy using server-only `FRED_API_KEY`, returns 30yr+15yr series with 12-week history + WoW deltas; Supabase session auth-gated — 401 on missing session; per-user sliding-window rate limit 30 req/min — 429 with `Retry-After`; edge-cached `s-maxage=3600, stale-while-revalidate=86400` + `Vary: Cookie`), `/api/property-lookup` (Zillow via `zillow-com-live-data-scraper-api` RapidAPI host using server-only `RAPIDAPI_KEY`; degraded-open auth — logs+continues if no session in dev, 401 in prod; per-user sliding-window rate limit 20 req/min — 429 with `Retry-After` + `errorType:'quota'`; 60s maxDuration for cold-start tolerance with sanitized timing logs; POST→`{ properties }` batch address lookups, GET→`{ property }` for `?address=` or `?mlsid=` single lookup; returns `errorType` for granular client handling), `/api/google-api-key` (removed — client now reads `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` directly; route returns 410 Gone for legacy callers)
- Lib: `lib/fred.ts` (server-only — reads `FRED_API_KEY` at request time; multi-series helper), `lib/zillow.ts` (server-only — reads `RAPIDAPI_KEY` + configurable host via `NEXT_PUBLIC_RAPIDAPI_ZILLOW_HOST`; exports `fetchPropertyByAddress`, `fetchPropertyByMlsId` (/bymlsid endpoint), and parallel-address helper; implements 30s timeout, 3-attempt exponential backoff with jitter (2s→4s→8s), address validation (min 10 chars, state/ZIP pattern), granular error classification with detailed diagnostic logging: timestamps + address prefix + attempt number + error type), `lib/google-places.ts` (client-side memoized helpers: **`loadGoogleMaps` uses tagged-result API** (`{ ok: true, loader }` or `{ ok: false, reason: 'noKey' | 'error', error? }`) with **debug logging** for `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` presence + loader init; loads `@googlemaps/js-api-loader` with `places` library; `loadGooglePlaces` returns Places Autocomplete service; includes loading/error states for graceful degradation), `lib/utils.ts` (cn, currency/date/number/percent formatters, `daysBetween`, `calcMonthlyPayment`, `maxHomePriceForIncome`), `lib/supabase/{client,server}.ts` (both read `NEXT_PUBLIC_SUPABASE_SCHEMA`; server client used by property-lookup + mortgage-rate routes for session-based auth check)
- Types: shared `types/index.ts` (incl. `ComparisonProperty`, `ComparisonResult` with reportNotes+clientName, extended `PipelineEntry`, `RateSeries`, `LocalMarketInputs`, `LocalMarketInputsData`, `MortgageCalc*`, `AgentBranding` (no googleApiKey field), `GooglePlace` with place_id+description, `AddressInputValue` union (string | GooglePlace), plus type guards `isGooglePlace`/`isMLSId`)
- Config: Tailwind tokens (Space Grotesk display, Inter body, JetBrains Mono accents, elevated shadow tokens, grain overlay utility), PostCSS, `next.config.js` (strict builds — no TS/ESLint bypass flags), `package.json` (next pinned to 14.2.33 — valid published security patch; `@googlemaps/js-api-loader` + `@types/google.maps` for Places integration), `.env.example` (Supabase shared-schema via `NEXT_PUBLIC_SUPABASE_SCHEMA` + server-only `FRED_API_KEY`, `RAPIDAPI_KEY`; `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` for client-side Google Maps/Places (no profile storage, read directly by client); non-sensitive `NEXT_PUBLIC_RAPIDAPI_ZILLOW_HOST` for Zillow host; **expanded Google Places troubleshooting** with tagged-result API explanation, debug logging locations (lib/google-places.ts + AddressInputs), and step-by-step inspection guide for key presence → loader init → autocomplete service → input attachment → place selection; comprehensive Zillow/RapidAPI troubleshooting), README (documents server-only vs public env var conventions + `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` direct-read pattern with no profile storage or API route + expanded Property Comparator section: Vercel cold-start expectations with 60s route timeout, exponential-backoff retry strategy, per-card vs bulk retry UX, address-vs-MLS-ID validation rules, Google Places Autocomplete integration with graceful degradation, and actionable steps for not-found vs connection/auth/quota errors mapped to yellow/red card states), `globals.css` (`html, body { overflow-x: hidden }` for global horizontal scroll prevention; print media query hides nav/topbar, forces light tokens, exposes page-break utilities)