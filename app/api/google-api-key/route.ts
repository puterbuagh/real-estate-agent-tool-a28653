// This route has been intentionally removed.
//
// The Google Maps API key is now read directly on the client from
// NEXT_PUBLIC_GOOGLE_MAPS_API_KEY. There is no server-side endpoint that
// resolves or returns the key. This file remains only as an empty module
// placeholder; Next.js will not register a route handler because no HTTP
// method exports are defined.
//
// If you are looking for the key loader, see `lib/google-places.ts`.

export {};
